<div>
<% @events.each do | event | %>

	<%= link_to teams_path(event), :class => "btn btn-primary btnCustom" do %>
		<div class="event-text"><%= event.day_of_week %> @ <%= event.time %>:00</div>
	<% end %>
<% end %>
</div>