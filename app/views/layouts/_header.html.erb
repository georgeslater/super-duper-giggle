  <header>
        <nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
            <div class="container-fluid">        
                <div class="navbar-header">
                    <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#menuContent">
                        <span class="sr-only">Toggle navigation</span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                    </button>
                    <div class="expatTitle">
                        <a id="obscureIcon" href="/">
                            <h1>Expat Montevideo Admin Zone</h1>
                        </a>
                    </div>
                </div>
                <div id="menuContent" class="collapse navbar-collapse">
                    <div id="siteLinks">
                        <ul id="menuBar" class="nav navbar-nav">
                            <li class="<%= 'activeTab' if current_page?(main_app.events_path) || current_page?(main_app.root_path) %>">
                                <%= link_to "Events", main_app.events_path %>
                            </li>
                            <li class="<%= 'activeTab' if current_page?(main_app.players_path) %>">
                                <%= link_to "Players", main_app.players_path %>
                            </li>                    
                        </ul>
                    </div>
                    <div>
                        <% if current_user %>
						    Welcome <strong><%= current_user.name %></strong>!
						    <%= link_to "Sign out", signout_path, id: "sign_out" %>
						  <% end %>
                    </div>
                </div>               
            </div>
        </nav>
    </header>
  <script>
	window.fbAsyncInit = function() {
	  FB.init({
	    appId      : '457519237774707',
	    cookie     : true,  // enable cookies to allow the server to access 
	                        // the session
	    xfbml      : true,  // parse social plugins on this page
	    version    : 'v2.5' // use graph api version 2.5
	  });
	};

	// Load the SDK asynchronously
	  (function(d, s, id) {
	    var js, fjs = d.getElementsByTagName(s)[0];
	    if (d.getElementById(id)) return;
	    js = d.createElement(s); js.id = id;
	    js.src = "//connect.facebook.net/en_US/sdk.js";
	    fjs.parentNode.insertBefore(js, fjs);
	  }(document, 'script', 'facebook-jssdk'));

  </script>
</div>